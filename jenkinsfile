pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    sh 'g++ -o YOUR_SRN-1 hello.cpp' // Compile hello.cpp
                }
            }
        }

        stage('Test') {
            steps {
                script {
                    sh './YOUR_SRN-1'  // Run the compiled program
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    echo 'Deploying the application...'
                    // Deployment commands go here (if needed)
                }
            }
        }
    }

    post {
        failure {
            echo 'Pipeline failed'
        }
    }
}
